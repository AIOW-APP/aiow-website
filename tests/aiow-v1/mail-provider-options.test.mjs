import test from "node:test";
import assert from "node:assert/strict";
import { buildMailProviderOptions } from "../../lib/aiow-v1/mail-provider-options.mjs";
test("production rejects provider test mode and URL overrides before any HTTP",()=>{let calls=0;const fetchImpl=()=>{calls++;};for(const env of [{NODE_ENV:"production",AIOW_MAIL_PROVIDER_TEST_MODE:"1",AIOW_MICROSOFT_TOKEN_URL:"http://127.0.0.1/token",AIOW_MICROSOFT_GRAPH_SEND_URL:"http://127.0.0.1/send"},{VERCEL_ENV:"production",AIOW_MAIL_PROVIDER_TEST_MODE:"1"},{NODE_ENV:"production",AIOW_MICROSOFT_TOKEN_URL:"https://login.example/token"}])assert.throws(()=>buildMailProviderOptions(env),/forbidden in production/);assert.equal(calls,0);void fetchImpl;});
test("non-production mock URLs are loopback-only and explicit",()=>{const options=buildMailProviderOptions({NODE_ENV:"test",AIOW_MAIL_PROVIDER_TEST_MODE:"1",AIOW_MICROSOFT_TOKEN_URL:"http://127.0.0.1/token",AIOW_MICROSOFT_GRAPH_SEND_URL:"http://localhost/send"});assert.equal(options.tokenUrl,"http://127.0.0.1/token");assert.equal(options.graphUrl,"http://localhost/send");assert.throws(()=>buildMailProviderOptions({NODE_ENV:"test",AIOW_MAIL_PROVIDER_TEST_MODE:"1",AIOW_MICROSOFT_TOKEN_URL:"http://attacker.example/token"}),/invalid provider mock URL/);});

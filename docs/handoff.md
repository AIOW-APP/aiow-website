# AIOW Website — Handoff

## What another agent needs to know

This is the AIOW Next.js website repo and already contains Team OS v2 project ops under `ops/`.

## Current caution

The working tree is dirty with many local changes/untracked files. Do not deploy, reset, or delete without review and approval boundaries.

## Start here

1. Read `STATUS.md`.
2. Read `ops/status/TEAM-OPERATING-SYSTEM-V2.md`.
3. Run `npm run build` before claiming readiness.
4. Put proof in `ops/evidence/` or link it from `STATUS.md`.

## Communication rule

No noisy progress claims. Report proof-backed status changes, blockers, or approval-needed items only.

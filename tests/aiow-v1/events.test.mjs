import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";import {validateContractDefinition} from "../../lib/aiow-v1/commercial-api-runtime.mjs";const fixtures=JSON.parse(await readFile(new URL("../fixtures/aiow-commercial-contract-v1.json",import.meta.url),"utf8"));
test("all closed analytics variants accept only their frozen shape",()=>{for(const [name,event] of Object.entries(fixtures.events)){assert.equal(validateContractDefinition("AnalyticsEvent",event),true,name);for(const hostile of [{...event,email:"person@example.com"},{...event,route:"/?email=a@b.io"},{...event,referrer:"https://evil.invalid"},{...event,metadata:{email:"a@b.io"}}])assert.equal(validateContractDefinition("AnalyticsEvent",hostile),false,`${name} hostile`);}});
test("route locale and experiment pairs are correlated",()=>{const nl=fixtures.events.page_view;assert.equal(validateContractDefinition("AnalyticsEvent",{...nl,route:"/en"}),false);const exposed=fixtures.events.experiment_exposed;assert.equal(validateContractDefinition("AnalyticsEvent",{...exposed,experiment:{experimentId:"other",variant:"control"}}),false);assert.equal(validateContractDefinition("AnalyticsEvent",{...exposed,experiment:{experimentId:"scan_cta_copy_v1",variant:"invented"}}),false);});
